'use client'
import { useState } from 'react'
import { createClient } from '@/lib/supabase/client'
import { useRouter } from 'next/navigation'
import { ArrowLeft, Loader2, Eye, Code, Save } from 'lucide-react'
import Link from 'next/link'
const DEFAULT_HTML = '<div style="font-family:system-ui;line-height:1.7"><div style="text-align:center;margin-bottom:2rem"><h2 style="color:#064e3b;margin:0">Beatrice Loving Heart</h2><p style="color:#6b7280;font-size:0.85rem">Home Health and DDA Services</p></div><p>Dear {{candidate_name}},</p><p>We are pleased to offer you the position of <strong>{{position_title}}</strong> in the <strong>{{department}}</strong> department.</p><p>Your compensation will be <strong>{{salary}}</strong>.</p><p>Start date: <strong>{{start_date}}</strong>.</p><p><strong>Benefits:</strong> {{benefits}}</p><p>This offer is contingent upon background check and credential verification.</p><p>Please sign below to accept.</p><p style="margin-top:2rem">Sincerely,<br/><strong>BLH HR Team</strong></p></div>'
export default function NewTemplatePage() {
  const [name,setName]=useState('');const [desc,setDesc]=useState('');const [cat,setCat]=useState('general')
  const [html,setHtml]=useState(DEFAULT_HTML);const [loading,setLoading]=useState(false);const [error,setError]=useState('')
  const [expDays,setExpDays]=useState('5');const router=useRouter();const supabase=createClient()
  async function handleSave(e:React.FormEvent){
    e.preventDefault();setLoading(true);setError('')
    const mergeFields = (html.match(/\{\{(\w+)\}\}/g)||[]).map((m: string)=>m.replace(/[{}]/g,''))
    const {error:err}=await supabase.from('offer_letter_templates').insert({name,description:desc,category:cat,html_content:html,merge_fields:mergeFields,expiration_days:parseInt(expDays)||5,is_active:true})
    if(err){setError(err.message);setLoading(false);return}
    router.push('/offers/templates');router.refresh()
  }
  const previewHTML = html.replace(/\{\{candidate_name\}\}/g,'Jane Smith').replace(/\{\{position_title\}\}/g,'Support Planner').replace(/\{\{department\}\}/g,'Community First Choice').replace(/\{\{salary\}\}/g,'$45,000/year').replace(/\{\{start_date\}\}/g,'July 1, 2026').replace(/\{\{benefits\}\}/g,'Health/Vision/Dental, 401k match, PTO, EAP')
  return (<div><div className="mb-6"><Link href="/offers/templates" className="inline-flex items-center gap-1 text-sm text-slate-500 hover:text-emerald-600 mb-3"><ArrowLeft className="w-4 h-4"/>Back to templates</Link><h1 className="text-2xl font-semibold text-slate-900">Create template</h1><p className="text-sm text-slate-500 mt-1">Build a reusable offer letter template with merge fields</p></div><form onSubmit={handleSave} className="space-y-6 max-w-3xl"><div className="bg-white rounded-2xl border border-slate-100 shadow-sm p-5 space-y-4"><div><label className="block text-sm font-medium text-slate-700 mb-1">Template name *</label><input type="text" required value={name} onChange={e=>setName(e.target.value)} placeholder="e.g. Standard Full-Time Offer" className="w-full px-3 py-2 border border-slate-200 rounded-xl text-sm focus:outline-none focus:ring-2 focus:ring-emerald-500/20 focus:border-emerald-500"/></div><div><label className="block text-sm font-medium text-slate-700 mb-1">Description</label><input type="text" value={desc} onChange={e=>setDesc(e.target.value)} className="w-full px-3 py-2 border border-slate-200 rounded-xl text-sm focus:outline-none focus:ring-2 focus:ring-emerald-500/20 focus:border-emerald-500"/></div><div className="grid grid-cols-2 gap-3"><div><label className="block text-sm font-medium text-slate-700 mb-1">Category</label><select value={cat} onChange={e=>setCat(e.target.value)} className="w-full px-3 py-2 border border-slate-200 rounded-xl text-sm"><option value="general">General</option><option value="full_time">Full time</option><option value="part_time">Part time</option><option value="specialized">Specialized</option></select></div><div><label className="block text-sm font-medium text-slate-700 mb-1">Expires (days)</label><input type="number" value={expDays} onChange={e=>setExpDays(e.target.value)} className="w-full px-3 py-2 border border-slate-200 rounded-xl text-sm"/></div></div></div><div className="bg-white rounded-2xl border border-slate-100 shadow-sm p-5"><label className="block text-sm font-medium text-slate-700 mb-2">HTML content (use merge fields like {'{{candidate_name}}'})</label><textarea value={html} onChange={e=>setHtml(e.target.value)} rows={14} className="w-full px-3 py-2 border border-slate-200 rounded-xl text-xs font-mono focus:outline-none focus:ring-2 focus:ring-emerald-500/20 focus:border-emerald-500"/></div><div className="bg-white rounded-2xl border border-slate-100 shadow-sm p-5"><h3 className="text-sm font-medium text-slate-900 mb-3 flex items-center gap-2"><Eye className="w-4 h-4 text-slate-400"/>Preview</h3><div className="prose prose-sm max-w-none border border-slate-100 rounded-xl p-6 bg-white" dangerouslySetInnerHTML={{__html:previewHTML}}/></div>{error&&<div className="text-sm text-red-600 bg-red-50 rounded-xl px-4 py-2.5">{error}</div>}<div className="flex gap-3"><Link href="/offers/templates" className="flex-1 text-center px-4 py-2.5 border border-slate-200 rounded-xl text-sm font-medium text-slate-700 hover:bg-slate-50">Cancel</Link><button type="submit" disabled={loading} className="flex-1 flex items-center justify-center gap-2 px-4 py-2.5 bg-gradient-to-r from-emerald-500 to-teal-600 text-white rounded-xl text-sm font-medium hover:from-emerald-600 hover:to-teal-700 disabled:opacity-50 shadow-lg shadow-emerald-500/20">{loading?<Loader2 className="w-4 h-4 animate-spin"/>:<><Save className="w-4 h-4"/>Save template</>}</button></div></form></div>)
}
