import { createClient } from '@/lib/supabase/server'
import { FileText, Plus, Layers } from 'lucide-react'
import Link from 'next/link'
export default async function TemplatesPage() {
  const s = await createClient()
  const { data: templates } = await s.from('offer_letter_templates').select('id, name, description, category, is_default, created_at').eq('is_active', true).order('category').order('name')
  return (<div><div className="flex items-center justify-between mb-6"><div><h1 className="text-2xl font-semibold text-slate-900">Offer Letter Templates</h1><p className="text-sm text-slate-500 mt-1">{templates?.length||0} templates</p></div><Link href="/offers/templates/new" className="flex items-center gap-2 px-4 py-2.5 bg-gradient-to-r from-emerald-500 to-teal-600 text-white rounded-xl text-sm font-medium hover:from-emerald-600 hover:to-teal-700 shadow-lg shadow-emerald-500/20"><Plus className="w-4 h-4"/>New Template</Link></div>{!templates?.length?(<div className="bg-white rounded-2xl border border-slate-100 p-12 shadow-sm text-center"><Layers className="w-12 h-12 text-slate-300 mx-auto mb-3"/><p className="text-sm text-slate-500">No templates yet. Create your first offer letter template.</p></div>):(<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">{templates.map((t:any)=>(<div key={t.id} className="bg-white rounded-2xl border border-slate-100 p-5 shadow-sm hover:shadow-md hover:border-emerald-200 transition-all cursor-pointer group"><div className="flex items-start justify-between mb-3"><div className="w-10 h-10 rounded-xl bg-violet-50 flex items-center justify-center group-hover:bg-violet-100 transition-colors"><FileText className="w-5 h-5 text-violet-600"/></div>{t.is_default&&<span className="text-[9px] uppercase tracking-wider text-emerald-600 bg-emerald-50 px-2 py-0.5 rounded-full border border-emerald-200">Default</span>}</div><h3 className="text-sm font-semibold text-slate-900 mb-1">{t.name}</h3><p className="text-xs text-slate-400 mb-3 line-clamp-2">{t.description||'No description'}</p><div className="flex items-center gap-2"><span className="text-[10px] px-2 py-0.5 rounded-full bg-slate-100 text-slate-600">{t.category||'General'}</span></div></div>))}</div>)}</div>)
}