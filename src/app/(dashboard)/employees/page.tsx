import { createClient } from '@/lib/supabase/server'
import { Users, Search, Plus } from 'lucide-react'
export default async function EmployeesPage() {
  const s = await createClient()
  const { data: emps } = await s.from('employees').select('id, first_name, last_name, email, phone, employment_status, hire_date, department:departments(name), role:roles(title)').eq('is_active', true).order('last_name')
  return (<div><div className="mb-6"><h1 className="text-2xl font-semibold text-slate-900">Employee Directory</h1><p className="text-sm text-slate-500 mt-1">{emps?.length||0} active employees</p></div><div className="bg-white rounded-2xl border border-slate-100 shadow-sm overflow-hidden">{!emps?.length?<div className="py-16 text-center"><Users className="w-10 h-10 text-slate-300 mx-auto mb-3"/><p className="text-sm text-slate-500">No employees yet. Add your first employee to get started.</p></div>:<div className="overflow-x-auto"><table className="w-full"><thead><tr className="border-b border-slate-100"><th className="text-left text-xs font-medium text-slate-500 uppercase tracking-wider px-5 py-3">Employee</th><th className="text-left text-xs font-medium text-slate-500 uppercase tracking-wider px-5 py-3">Department</th><th className="text-left text-xs font-medium text-slate-500 uppercase tracking-wider px-5 py-3">Role</th><th className="text-left text-xs font-medium text-slate-500 uppercase tracking-wider px-5 py-3">Status</th></tr></thead><tbody className="divide-y divide-slate-50">{(emps as any[]).map(e=>(<tr key={e.id} className="hover:bg-slate-50/50"><td className="px-5 py-3.5"><div className="flex items-center gap-3"><div className="w-9 h-9 rounded-full bg-gradient-to-br from-emerald-400 to-teal-500 flex items-center justify-center text-white text-xs font-medium">{(e.first_name[0]+e.last_name[0]).toUpperCase()}</div><div><p className="text-sm font-medium text-slate-900">{e.first_name} {e.last_name}</p><p className="text-xs text-slate-400">{e.email}</p></div></div></td><td className="px-5 py-3.5 text-sm text-slate-600">{e.department?.name||'—'}</td><td className="px-5 py-3.5 text-sm text-slate-600">{e.role?.title||'—'}</td><td className="px-5 py-3.5"><span className="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-emerald-50 text-emerald-700 border border-emerald-200">{e.employment_status}</span></td></tr>))}</tbody></table></div>}</div></div>)
}