import { createClient } from '@/lib/supabase/server'
import { Briefcase, Users, Shield, Stethoscope, HeartPulse, FlaskConical } from 'lucide-react'
export default async function RolesPage() {
  const s = await createClient()
  const { data: roles } = await s.from('roles').select('*, department:departments(id, name)').eq('is_active', true).order('title')
  const { data: emps } = await s.from('employees').select('role_id').eq('is_active', true)
  const rc:any={};emps?.forEach(e=>{if(e.role_id)rc[e.role_id]=(rc[e.role_id]||0)+1})
  return (<div><div className="mb-6"><h1 className="text-2xl font-semibold text-slate-900">Roles</h1><p className="text-sm text-slate-500 mt-1">{roles?.length||0} active roles</p></div><div className="bg-white rounded-2xl border border-slate-100 shadow-sm overflow-hidden"><div className="overflow-x-auto"><table className="w-full"><thead><tr className="border-b border-slate-100"><th className="text-left text-xs font-medium text-slate-500 uppercase tracking-wider px-5 py-3">Role</th><th className="text-left text-xs font-medium text-slate-500 uppercase tracking-wider px-5 py-3">Department</th><th className="text-left text-xs font-medium text-slate-500 uppercase tracking-wider px-5 py-3">Employees</th><th className="text-left text-xs font-medium text-slate-500 uppercase tracking-wider px-5 py-3">Compliance</th></tr></thead><tbody className="divide-y divide-slate-50">{roles?.map((r:any)=>(<tr key={r.id} className="hover:bg-slate-50/50"><td className="px-5 py-3.5"><div className="flex items-center gap-3"><div className="w-8 h-8 rounded-lg bg-violet-50 flex items-center justify-center"><Briefcase className="w-4 h-4 text-violet-600"/></div><p className="text-sm font-medium text-slate-900">{r.title}</p></div></td><td className="px-5 py-3.5 text-sm text-slate-600">{r.department?.name||'—'}</td><td className="px-5 py-3.5"><span className="flex items-center gap-1 text-sm text-slate-600"><Users className="w-3.5 h-3.5 text-slate-400"/>{rc[r.id]||0}</span></td><td className="px-5 py-3.5"><div className="flex items-center gap-1.5 flex-wrap">{r.requires_nursing_license&&<span className="inline-flex items-center gap-1 text-[10px] font-medium bg-pink-50 text-pink-700 border border-pink-200 rounded-full px-2 py-0.5"><Stethoscope className="w-3 h-3"/>Nursing</span>}{r.requires_cpr_cert&&<span className="inline-flex items-center gap-1 text-[10px] font-medium bg-red-50 text-red-700 border border-red-200 rounded-full px-2 py-0.5"><HeartPulse className="w-3 h-3"/>CPR</span>}{r.requires_tb_test&&<span className="inline-flex items-center gap-1 text-[10px] font-medium bg-amber-50 text-amber-700 border border-amber-200 rounded-full px-2 py-0.5"><FlaskConical className="w-3 h-3"/>TB</span>}{r.requires_background_check&&<span className="inline-flex items-center gap-1 text-[10px] font-medium bg-slate-100 text-slate-600 border border-slate-200 rounded-full px-2 py-0.5"><Shield className="w-3 h-3"/>BG</span>}</div></td></tr>))}</tbody></table></div></div></div>)
}